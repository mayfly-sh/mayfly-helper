# Managed by mayfly-agent. Do not edit; changes are overwritten.
# Configures the Mayfly-managed OpenSSH user-certificate trust anchor.
#
# This drop-in is installed/refreshed by mayfly-helper. For it to take effect,
# the main /etc/ssh/sshd_config must contain:
#     Include /etc/ssh/sshd_config.d/*.conf
# (the OpenSSH default on modern distributions). mayfly-helper detects a missing
# Include and refuses to proceed rather than silently producing a no-op config.
TrustedUserCAKeys /etc/ssh/mayfly/trusted_user_ca_keys
